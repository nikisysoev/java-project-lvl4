datasource:
  default: ${APP_ENV}
  development:
    username: sa
    password: sa
    url: "jdbc:h2:./database"
  production:
    username: ${JDBC_DATABASE_URL}
    password: ${JDBC_DATABASE_URL}
    url: ${JDBC_DATABASE_URL}

ebean:
  migration:
    run: true
